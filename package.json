{
  "name": "each-component",
  "version": "0.0.1",
  "description": "Array / object / string iteration utility",
  "dependencies": {
    "type": "component/type"
  },
  "devDependencies": {
    "mocha": "*",
    "should": ">=11.1.1"
  },
  "component": {
    "scripts": {
      "each/index.js": "index.js"
    }
  }
}
